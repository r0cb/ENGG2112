# Data Dictionary — `master_stacked.csv`

Auto-generated by Notebook 01. Each row = `(county, disease)` observation.

Shape: 357 rows × 39 cols
Diseases: ['COVID', 'FLU', 'RSV']
States: ['CT', 'DE', 'NY', 'PA']

See `REFERENCES.md` for source citations.

## Identity

| Column | Type | Description |
|---|---|---|
| `state` | string | Two-letter state code (NY/PA/CT/DE) |
| `state_fips` | string | 2-digit state FIPS |
| `fips` | string | 5-digit county/area FIPS |
| `county` | string | County or planning region name |
| `NAME` | string | Full Census-formatted area name |
| `disease` | string | FLU / COVID / RSV — categorical disease type |
| `season` | string | Season label (e.g. 2024-2025) |
| `season_start_year` | int | Year season started |

## Outcome

| Column | Type | Description |
|---|---|---|
| `outcome_per_100k` | float | Primary outcome — disease cases per 100K population |
| `total_cases` | float | Total raw case count (FLU/RSV only) |
| `total_cases_per_100k` | float | Cumulative weekly cases per 100K (COVID only — endemic season Jun 2022–May 2023) |
| `peak_week_cases` | float | Maximum single-week count (FLU/RSV only) |
| `peak_week_cases_per_100k` | float | Highest single-week rate per 100K (COVID only) |
| `weeks_active` | float | Weeks with non-zero cases |
| `pct_weeks_high_level` | float | % of weeks at CDC High community level (COVID only) |

## Feature

| Column | Type | Description |
|---|---|---|
| `pop_density_per_sqmi` | float | Population / land area (urban-ness) |
| `median_age` | float | ACS B01002_001E |
| `pct_elderly` | float | Pop 65+ / total × 100 |
| `avg_household_size` | float | ACS B25010_001E |
| `median_income` | float | Median household income (USD) |
| `poverty_rate` | float | Below poverty / total × 100 |
| `unemployment_rate` | float | Unemployed / labour force × 100 |
| `public_transport_pct` | float | Public transport commuters / total × 100 |
| `pct_bachelors_plus` | float | Bachelor's+ / total × 100 |
| `pct_non_white` | float | (Total - white) / total × 100 |
| `pct_foreign_born` | float | Foreign-born / total × 100 |

## Feature (PLACES)

| Column | Type | Description |
|---|---|---|
| `pct_checkup` | float | % adults with annual checkup — preventive care engagement |
| `pct_obesity` | float | % adults obese — flu severity factor |
| `pct_diabetes` | float | % adults with diabetes — comorbidity |
| `pct_copd` | float | % adults with COPD — direct lung comorbidity |
| `pct_chd` | float | % adults with heart disease — comorbidity |
| `pct_csmoking` | float | % adults currently smoking |

## Raw

| Column | Type | Description |
|---|---|---|
| `pop_total` | int | Total population |
| `land_area_sqmi` | float | Land area (square miles) |

## Label

| Column | Type | Description |
|---|---|---|
| `outbreak` | int | Top 25% within (disease, state) by outcome_per_100k |

